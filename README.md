# ANN model on Fashtion_mnist_dataset
We build an model base on the tensorflow package in which use keras, feach the dataset from keras,check dataset shape,normalize data and build an ANN model predict which image has which class and index on training data of 60,000images.
